user nginx;  # o www-data kung Debian/Ubuntu
worker_processes auto;

error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    keepalive_timeout 65;

    # ⚠️ MAHALAGA: Para gumana ang WS/HTTP Upgrade at VLESS
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    server {
        listen 80;
        server_name localhost; # palitan ng iyong domain/ip

        # Normal na website
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ =404;
        }

        # Endpoint para sa config.json
        location /config {
            default_type application/json;
            root /app;
            try_files /config.json =404;
        }

        # Halimbawa: Kung ipapasa sa VLESS WS port (halimbawa 8080)
        # location /vless-ws {
        #     proxy_pass http://127.0.0.1:8080;
        # }
    }
}
